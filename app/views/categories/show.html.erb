<% header = link_to('All Posts', posts_path) + " &raquo; Categories: #{@category.name}".html_safe %>
<%= render 'shared/content_title', title: header %>
<% @category.posts.each do |post| %>
  <%= render post %>
<% end %>
