<% if @vote.valid? %>
  $('#comment_<%= @comment.slug %>_votes').html('<%= pluralize(@comment.net_votes, "vote") %>');
<% else %>
$('#main').before("<div class='alert alert-error'>\
<a class='close' data-dismiss='alert'>x</a>\
<div>You can only vote once.</div></div>");
<% end %>
