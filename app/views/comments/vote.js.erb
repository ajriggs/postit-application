<% if @vote.valid? %>
  $('#comment_<%= @comment.id %>_votes').html('<%= pluralize(@comment.net_votes, "vote") %>');
<% else %>
  $('#comment_vote_error').show();
  $('html, body').animate({ scrollTop: 0 }, 0);
<% end %>
